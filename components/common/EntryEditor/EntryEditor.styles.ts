import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const EntryEditorContainer = styled(Box)({
    borderBottom: "1px solid rgba(0, 0, 0, 0.12)",
    padding:"16px",
    background:"#ffffff",
    marginBottom:"32px"
})