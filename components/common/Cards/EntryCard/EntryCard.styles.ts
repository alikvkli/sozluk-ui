import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const EntryCardContainer = styled(Box)({
        borderBottom:"1px solid rgba(0, 0, 0, 0.12)",
        padding:"16px"
})